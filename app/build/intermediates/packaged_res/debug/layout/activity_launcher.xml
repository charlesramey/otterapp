<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- App Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Otter Enrichment"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="24dp" />

    <!-- Welcome Message -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Welcome! Are you a first-time user or an experienced user?"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp" />

    <!-- First-Time User Button -->
    <Button
        android:id="@+id/btnFirstTimeUser"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="First-Time User (Tutorial)"
        android:textSize="16sp"
        android:layout_marginBottom="16dp"
        style="@style/Widget.Material3.Button" />

    <!-- Experienced User Button -->
    <Button
        android:id="@+id/btnExperiencedUser"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Experienced User"
        android:textSize="16sp"
        style="@style/Widget.Material3.Button.TonalButton" />

</LinearLayout>
